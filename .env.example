# Optional: DATABASE_URL is only needed if you use the Prisma scaffold
DATABASE_URL="file:./db/custom.db"

# No GEMINI_API_KEY or other API key needed — the app uses z-ai-web-dev-sdk with a bypass key.
# In production deployments, the SDK auto-resolves credentials from the runtime environment.
